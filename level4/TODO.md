# Level 4 — Symbolically Supervised Neural Model
## Implementation TODO

| # | Area | Task | Notes | Status |
|---|------|------|-------|--------|
| 1 | Environment | Confirm `sentence-transformers` (`all-MiniLM-L6-v2`) is available in venv | Installed v5.4.1; model downloaded, embedding dim=384 | Done |
| 2 | Data | Audit `intents_base.csv` — confirm utterance + intent coverage | 1,661 rows, 2 cols (utterance, intent), no nulls, balanced across 4 intents; needs entity_type + domain_valid columns | Done |
| 3 | Data | Programmatically generate `entity_type` and `domain_valid` labels using existing `level3_5` EntityMatcher and ontology | Done — 1,661 rows labeled; entity_type mapped to 7 categories (infrastructure/service/metric/incident/job/pipeline/unknown); 110 SRE-intent rows with domain_valid=False (ontology coverage gaps, kept as honest signal); saved to `level4/data/labeled_raw.csv` | Done |
| 4 | Data | Manually review and correct generated labels | 224 out_of_scope rows: spurious TF-IDF entity matches corrected to `unknown`. 110 SRE-intent rows that had domain_valid=False corrected to domain_valid=True (were ontology grounding gaps, not truly out-of-domain — entity_type stays `unknown`, constraint loss handles grounding weakness). Saved to `level4/data/labeled_clean.csv`. Audit confirmed clean separation: all SRE intents → domain_valid=True, all out_of_scope → domain_valid=False, 0 constraint violations in labels. | Done |
| 5 | Data | Split into `train.csv` and `test.csv` | Stratified 80/20 split by intent (after label correction); train=1328 rows, test=333 rows; intent proportions preserved across both splits | Done |
| 5a | Data | Add `data_audit.py` pre-training validation script | Prints distributions by intent/entity/domain_valid, cross-tabs, and counts constraint violations in labels. Run with `--split clean\|train\|test`. Confirmed: 0 violations in labels, clean domain_valid separation. | Done |
| 6 | Ontology | Create `ontology/constraint_rules.json` — define allowed and disallowed intent/entity_type pairs | 10 disallowed rules: 6 TYPE_A false-rejection (out_of_scope + SRE entity, weight=1.0), 2 TYPE_B false-execution (execution + incident/metric, weight=0.75 — metric removed from execution's allowed list), 2 TYPE_C ungrounded SRE (unknown entity for investigate/summarization, weight=0.5). Added `_domain_valid_semantics` note: domain_valid=False maps only to out_of_scope; TYPE_C handles grounding gaps separately. | Done |
| 7 | Model | Implement `model/dataset.py` — PyTorch Dataset class over `train.csv` / `test.csv` | Label vocab matches constraint_rules.json; validates unknown intents/entities at load time; returns utterance text + 3 label tensors | Done |
| 8 | Model | Implement `model/neural_intent_model.py` — all-MiniLM encoder (frozen) + shared dense + intent/entity/domain heads | 384→256 shared trunk, 3 heads (4/7/1 outputs); `.clone()` on embeddings to exit inference mode for autograd; clean `predict()` with no symbolic post-processing | Done |
| 9 | Model | Implement `model/losses.py` — constraint violation loss | Differentiable soft penalty: sum over disallowed pairs of w_ij * mean(p_intent[:,i] * p_entity[:,j]); Level4Loss = intent_loss + α*entity_loss + β*domain_loss + λ*constraint_loss; lam=0 gives clean baseline | Done |
| 10 | Training | Implement `train.py` — training loop with `total_loss = intent_loss + α*entity_loss + β*domain_loss + λ*constraint_loss` | Custom collate_fn for str utterances; AdamW on trainable params only; per-epoch logging of all loss components + intent/entity/domain accuracy on train & val; saves best checkpoint by val intent accuracy | Done |
| 11 | Inference | Implement `infer.py` — clean inference path: `utterance → model → prediction` with zero symbolic post-processing | Single-utterance, CSV-file, and interactive REPL modes; `--json` output flag; `--output-file` JSONL writer; loads checkpoint metadata (lam, epoch, val_intent_acc) | Done |
| 12 | Evaluation | Implement `evaluation/violation_metrics.py` — compute constraint violation rate, false execution rate, false rejection rate | Computes intent/entity/domain accuracy + TYPE_A/B/C violation rates + per-pair counts; saves `evaluation_metrics.json` alongside checkpoint; offline only — never corrects predictions | Done |
| 13 | Experiment A | Train baseline neural model (λ = 0, no symbolic loss, no runtime guard) | val_intent_acc=0.972; violation rate=0.051 (17/333); TYPE_C=0.075 (investigate/summarization + unknown). Sets baseline for constraint violation measurement. | Done |
| 14 | Experiment B | Run Level 3.5 runtime pipeline on same test set and record metrics | intent_acc=0.931; entity_acc=0.372; domain_acc=0.288; TYPE_C violation rate=0.643 (runtime symbolic grounding fails on 64% of test set). Demonstrates that runtime post-hoc correction does not generalise. | Done |
| 15 | Experiment C | Train Level 4 model with symbolic constraint loss (λ > 0, no runtime guard) | λ=0.5: val_intent_acc=0.972; violation rate=0.048 (16/333); TYPE_C=0.075 — comparable accuracy to baseline neural, constraint loss already penalising violations during training | Done |
| 16 | Experiment D | λ ablation sweep: 0.0, 0.1, 0.25, 0.5, 1.0, 2.0 | Sweet spot appears at λ=1.0 (viol rate=0.036) and λ=2.0 (viol rate=0.021, TYPE_C=0.051). TYPE_B eliminated at λ≥0.1. Accuracy degrades only mildly at λ=2.0 (0.961 vs 0.970 baseline). | Done |
| 17 | Evaluation | Produce comparison table: Baseline / Level 3.5 / Level 4 across all metrics | Table produced across all 7 runs; key finding: Level 4 λ=1.0 achieves best violation reduction (0.036) while preserving intent accuracy (0.967) vs Level 3.5 violation rate 0.643. λ=2.0 further reduces violations (0.021) with minor accuracy cost. | Done |
| 18 | Documentation | Update `level3_5/README.md` — add explicit reclassification note | State it was originally Level 4, reclassified to 3.5 per Kautz typology review | Open |
| 19 | Documentation | Write `level4/README.md` — define the level, architecture, experiment design, and what success looks like | Include architecture diagram and paper framing | Open |
| 20 | Notebook | Create `level4/level4_symbolic_loss.ipynb` — end-to-end walkthrough of train → infer → evaluate | For presentation and reproducibility | Open |
