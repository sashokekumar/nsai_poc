# Level 5 — Symbolic Rules Compiled into Differentiable Neural Architecture
## Implementation TODO

| # | Area | Task | Notes | Status |
|---|------|------|-------|--------|
| 1 | Architecture | Define Level 5 typology and distinguish from Level 4 / 4.5 | Level 4: symbolic loss at training time, no rules at inference. Level 4.5: runtime post-hoc masking after softmax. Level 5: symbolic rules are structural components of the network — compiled into a differentiable rule layer that sits between predicate heads and final intent output. Rules influence forward pass; weights are learned. | Done |
| 2 | Data | Audit `level4/data/labeled_clean.csv` as base dataset | 1,661 rows; columns: utterance, intent, entity_type, domain_valid. Needs 11 predicate columns added (7 entity-type indicators + 4 signal predicates: is_sre_domain, has_runbook, is_known_incident, is_metric_query). Confirmed: 0 nulls, 4 intents, 7 entity_type values. | Done |
| 3 | Data | Derive predicate labels from existing tooling | Entity-type predicates: direct one-hot from existing entity_type column (reliable, already audited). Signal predicates derived via keyword regex: is_sre_domain (33.7%), has_runbook (17.8% — 95% execution-intent, strong signal), is_known_incident (4.3% — 50% investigate), is_metric_query (7.1% — 55% investigate). Script: `level5/build_dataset.py`. Output: `level5/data/level5_labeled.csv` (1,661 rows, 15 cols). VERDICT: READY. | Done |
| 4 | Data | Define `level5/data/rule_base.json` — symbolic rule base | 4 rules: R1_metric_investigate (OR: is_metric_query, is_known_incident → investigate, init=0.7), R2_runbook_execution (AND: has_runbook, is_sre_domain → execution, init=0.9 — 95% signal), R3_incident_summarization (AND: is_known_incident, is_sre_domain, NOT has_runbook → summarization, init=0.6), R4_unknown_out_of_scope (AND: is_unknown, NOT is_sre_domain → out_of_scope, init=0.8). Nested logic tree schema. | Done |
| 5 | Model | Implement `level5/model/differentiable_logic.py` | Product t-norm operators: `logic_and(a, b) = a * b`, `logic_or(a, b) = a + b - a*b`, `logic_not(a) = 1 - a`. N-ary variants: `logic_and_multi`, `logic_or_multi`. All verified: AND(0.8,0.6)=0.480, OR(0.2,0.9)=0.920, NOT(0.8)=0.200. Fully differentiable — gradients flow through all operators. | Done |
| 6 | Model | Implement `level5/model/rule_compiler.py` — `RuleCompiler` class | Reads `rule_base.json`; recursively evaluates nested AND/OR/NOT logic trees over predicate_probs [B, 11] → rule_activations [B, 4]. One learnable `rule_strength_logit` per rule (sigmoid-bounded). `scatter_to_intents()` aggregates rule activations to per-intent scores via max. `rule_strength_dict()` for logging. Validated: correct shapes, init strengths match rule_base.json. | Done |
| 7 | Model | Implement `level5/model/level5_model.py` — `Level5IntentModel` | Architecture: frozen encoder (384) → shared trunk (384→256, ReLU, Dropout) → predicate_head (256→11, sigmoid) → RuleCompiler (11→4 rule activations) → intent logit blend: α * rule_scores + (1-α) * trunk_logits where α=rule_weight (learnable sigmoid parameter, init=0.5). Also: model/dataset.py (Level5Dataset, 11 predicate cols) and model/__init__.py. Full predict() returns intent + predicate_activations + rule_activations + rule_weight per utterance. Smoke-tested end-to-end. | Done |
| 8 | Model | Implement `level5/model/dataset.py` — PyTorch Dataset | Implemented as part of Task 7. `Level5Dataset`: loads `level5_labeled.csv`, validates all 11 predicate columns exist and are binary, validates intent vocab, returns utterance/intent_idx/predicate_labels per sample. 1,661 rows, 0 nulls. | Done |
| 9 | Model | Implement `level5/model/losses.py` — combined loss | `Level5Loss = CrossEntropy(intent_logits, targets) + pred_weight * BCELoss(predicate_probs, predicate_targets)`. No explicit constraint penalty — constraints are structural (baked into rule layer). pred_weight=0.5 default; set to 0.0 to disable predicate supervision. Returns dict with loss/intent_loss/predicate_loss. Verified: total=correct, gradients flow. | Done |
| 10 | Training | Implement `level5/train.py` — training loop | Stratified 80/20 split from level5_labeled.csv. AdamW on all trainable params (shared trunk + predicate head + rule_strength_logits + rule_weight_logit). Per-epoch logging: intent acc, predicate acc, per-head predicate acc, mean rule activations, rule_strength values, blend_weight. Saves best checkpoint by val intent accuracy with full metadata. CLI flags: --run-name, --epochs, --pred-weight, --freeze-rules, --rule-strength-init (for ablations). Backward pass verified: gradients flow through rule layer to predicate heads. | Done |
| 11 | Inference | Implement `level5/infer.py` — inference path | `utterance → encoder → trunk → predicate heads → rule layer → intent`. Single-utterance, CSV file, and REPL modes. Output per utterance: intent + intent_prob + predicate_activations (11 values) + rule_activations (4 values) + rule_weight. `format_result()` shows top-4 predicate activations and all rule activations in human-readable form. Checkpoint metadata (rule_strengths, blend_weight, val_acc) printed on load. Verified: run_inference, format_result, all output keys present. | Done |
| 12 | Evaluation | Implement `level5/evaluation/violation_metrics.py` | Computes: intent accuracy (overall + per-class), predicate accuracy (overall + per-head), rule fidelity (% predictions consistent with top-fired rule's consequent), mean rule activations, learned rule_strength dict, blend_weight. Ontology violations use same Level 4 constraint_rules.json for cross-level comparison. TYPE_A/B/C taxonomy preserved. Entity type inferred from predicate activations (argmax over is_* columns). Saves evaluation_metrics.json alongside checkpoint. --compare-l4 flag for side-by-side table. Verified: all 15 metric keys present, logic correct. | Done |
| 13 | Experiment A | Train baseline: rule_strength all initialised to 0 (rules disabled) | Verifies the trunk + predicate heads work independently. Expected: similar to Level 4 baseline accuracy. Sets predicate head quality floor. | Done |
| 14 | Experiment B | Train with rule_strength learnable from rule_base.json initialisations | Main Level 5 experiment. Expected: rules with strong antecedent→consequent signal (R1, R4) should converge rule_strength → 1.0; R2/R3 may be weaker. Compare intent accuracy and violation rate against Level 4 λ=2.0 (viol=0.021, acc=0.961). | Done |
| 15 | Experiment C | Ablation: freeze rule_strength vs learn rule_strength | Determines how much the network benefits from allowing rules to be soft vs hard. Frozen at 1.0 = hard symbolic, learnable = neuro-symbolic blend. | Done |
| 16 | Evaluation | Produce comparison table across all levels: L3.5 / L4 / L4.5 / L5 | Key metrics: intent accuracy, constraint violation rate, symbolic integration type (runtime / training-time / structural), rule_strength interpretability. | Done |
| 17 | Notebook | Create `level5/level5_rule_compiled_network.ipynb` — end-to-end walkthrough | Sections: architecture overview, predicate derivation, rule base definition, rule layer forward pass demo, training run, rule_strength convergence plots, violation rate comparison table vs L4/L4.5, summary. | Done |
| 18 | Documentation | Write `level5/README.md` — architecture, Kautz classification, experiment design | Include: ASCII architecture diagram showing rule layer position, math for product t-norm logic, rule_base.json schema, comparison table vs L3.5/L4/L4.5, reproduce commands, file structure. | Done |
